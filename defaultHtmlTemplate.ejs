<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%- title %></title>
    <style type="text/css">
        @import url('https://fonts.googleapis.com/css2?family=Noto+Serif:ital,wght@1,700&family=Roboto:ital,wght@0,300;0,700;1,300;1,700&display=swap');
        body {
            margin: 0; padding: 0;
            font-family: 'Roboto', sans-serif;
            font-weight: 300;
        }
        header {
            font-family: 'Noto Serif', serif;
            font-style: italic;
            font-size: 18pt;
            font-weight: 700;
            padding: 8pt 10%;
            background-color: #f0f0f0;
        }
        header a {
            color: black;
            text-decoration: none;
        }
        header h1 {
            margin: 0; padding: 0;
        }
        article {
            padding: 8pt 10%;
        }
        footer {
            background-color: #f0f0f0;
            padding: 10pt 10%;
            font-size: 80%;
            color: #777;
        }
        pre {
            visibility: hidden;
        }
    </style>
</head>
<body>
<header>
    <h1><a href="/index.html">Site Header</a></h1>
    <% if (Object.keys(metadata).length > 0) { %>
        <table>
            <tr>
                <th>Property</th>
                <th>Value</th>
            </tr>
            <% for (const prop in metadata) { %>
                <tr>
                    <td><%= prop %></td>
                    <td><%= metadata[prop] %></td>
                </tr>
            <% } %>
        </table>
    <% } %>
</header>
<article>
<%- content %>
</article>
<footer>
    <p>Site generated by zk-publisher.</p>
</footer>
</body>
</html>